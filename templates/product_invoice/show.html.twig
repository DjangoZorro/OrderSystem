{% extends 'base.html.twig' %}

{% block title %}ProductInvoice{% endblock %}

{% block body %}
    <h1>ProductInvoice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_invoice.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ product_invoice.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_invoice_index') }}">back to list</a>

    <a href="{{ path('product_invoice_edit', {'id': product_invoice.id}) }}">edit</a>

    {{ include('product_invoice/_delete_form.html.twig') }}
{% endblock %}
