{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Product index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Name</th>
                <th>Description</th>
                <th>Tax</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.code }}</td>
                <td>{{ product.name }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.tax }}</td>
                <td>{{ product.price }}</td>
                <td>
                    <a class="btn btn-secondary" href="{{ path('product_show', {'id': product.id}) }}">show</a>
                    <a class="btn btn-secondary" href="{{ path('product_edit', {'id': product.id}) }}">edit</a>
                    <a class="btn btn-success" href="{{ path('product_added', {'id': product.id}) }}">add</a>
                </td>
                <td>{{ include('product/_delete_form.html.twig') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('product_new') }}">Create new</a>
{% endblock %}
